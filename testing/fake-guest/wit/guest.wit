package clean:guest@0.1.0;

world app {
    import clean:host/routing@0.1.0;
    import clean:host/request@0.1.0;
    import clean:host/response@0.1.0;
    import clean:host/websocket@0.1.0;
    import clean:host/sse@0.1.0;
    import clean:host/log@0.1.0;
    /// Composed from testing/fake-bridge when configured.
    import clean:fake-bridge/store@0.1.0;

    export init: func();
    export handle: func(handler-id: u32);
}
